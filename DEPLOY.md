# 🚀 Развертывание бота на Яндекс.Облако (Ubuntu)

## Шаг 1: Подключение к серверу

1. Откройте PowerShell или терминал на вашем компьютере
2. Перейдите в папку, где лежит ваш SSH ключ (обычно `C:\Users\ваше_имя\.ssh\`)
3. Подключитесь к серверу:

```bash
ssh -i путь_к_ключу.pem ubuntu@ваш_внешний_IP
```

Например:
```bash
ssh -i C:\Users\uirya\.ssh\yandex-key.pem ubuntu@51.250.XX.XX
```

**Если не работает:**
- Убедитесь, что ключ имеет правильные права (на Windows обычно не нужно)
- Проверьте внешний IP в консоли Яндекс.Облака
- Убедитесь, что в настройках безопасности ВМ разрешён SSH (порт 22)

---

## Шаг 2: Обновление системы

После подключения выполните:

```bash
sudo apt update
sudo apt upgrade -y
```

---

## Шаг 3: Установка Python и зависимостей

```bash
# Установка Python 3.11 и pip
sudo apt install -y python3.11 python3.11-venv python3-pip git

# Проверка версии
python3 --version
```

---

## Шаг 4: Копирование проекта на сервер

### Вариант А: Через Git (если проект в репозитории)

```bash
# Установка Git (если ещё не установлен)
sudo apt install -y git

# Клонирование проекта
cd ~
git clone https://github.com/ваш_username/Girl-Guy.Ai.git
cd Girl-Guy.Ai
```

### Вариант Б: Через SCP (копирование с вашего компьютера)

**На вашем компьютере (в PowerShell):**

```powershell
# Перейдите в папку с проектом
cd C:\Users\uirya\Desktop\Girl-Guy.Ai

# Скопируйте проект на сервер
scp -i путь_к_ключу.pem -r * ubuntu@ваш_IP:~/bot/
```

Например:
```powershell
scp -i C:\Users\uirya\.ssh\yandex-key.pem -r * ubuntu@51.250.XX.XX:~/bot/
```

**На сервере:**

```bash
cd ~/bot
```

---

## Шаг 5: Создание виртуального окружения

```bash
# Создаём виртуальное окружение
python3 -m venv venv

# Активируем его
source venv/bin/activate

# Обновляем pip
pip install --upgrade pip
```

---

## Шаг 6: Установка зависимостей

```bash
# Устанавливаем все зависимости
pip install -r requirements.txt
```

---

## Шаг 7: Настройка переменных окружения

Создайте файл `.env`:

```bash
nano .env
```

Добавьте туда (замените на свои значения):

```env
# Обязательные переменные
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
TELEGRAM_BOT_USERNAME=имя_вашего_бота_без_@

# API ключ для AI (OpenRouter)
OPENROUTER_API_KEY=ваш_ключ_openrouter

# Опционально: настройки для платежей
TRIBUTE_API_KEY=ваш_ключ_tribute
STARS_API_KEY=ваш_ключ_stars

# Опционально: настройки хранения фото (Yandex Object Storage)
STORAGE_TYPE=yandex
YANDEX_BUCKET=имя_вашего_бакета
YANDEX_ACCESS_KEY_ID=ваш_access_key
YANDEX_SECRET_ACCESS_KEY=ваш_secret_key
YANDEX_REGION=ru-central1

# Опционально: настройки токенов
SMS_DEFAULT_TOKENS=20
REFERRAL_REWARD_TOKENS=15
```

**Как сохранить в nano:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

---

## Шаг 8: Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активно
source venv/bin/activate

# Запустите бота
python main.py
```

Если всё работает, остановите бота (`Ctrl + C`).

---

## Шаг 9: Настройка автозапуска (systemd)

Создайте сервис для автозапуска:

```bash
sudo nano /etc/systemd/system/bot.service
```

Добавьте туда:

```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/bot
Environment="PATH=/home/ubuntu/bot/venv/bin"
ExecStart=/home/ubuntu/bot/venv/bin/python /home/ubuntu/bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `/home/ubuntu/bot` на реальный путь к вашему проекту!

Сохраните (`Ctrl + O`, `Enter`, `Ctrl + X`).

Затем:

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable bot.service

# Запускаем бота
sudo systemctl start bot.service

# Проверяем статус
sudo systemctl status bot.service
```

---

## Шаг 10: Полезные команды

```bash
# Посмотреть логи бота
sudo journalctl -u bot.service -f

# Остановить бота
sudo systemctl stop bot.service

# Перезапустить бота
sudo systemctl restart bot.service

# Посмотреть статус
sudo systemctl status bot.service
```

---

## 🔧 Решение проблем

### Бот не запускается

1. Проверьте логи:
```bash
sudo journalctl -u bot.service -n 50
```

2. Проверьте, что `.env` файл существует и содержит все нужные переменные:
```bash
cat .env
```

3. Проверьте, что виртуальное окружение активно и зависимости установлены:
```bash
source venv/bin/activate
pip list
```

### Ошибка подключения к базе данных

База данных создаётся автоматически при первом запуске. Если есть проблемы:

```bash
# Проверьте права доступа
ls -la users.db
chmod 644 users.db
```

### Проблемы с правами доступа

```bash
# Дайте права на папку проекта
sudo chown -R ubuntu:ubuntu ~/bot
chmod +x ~/bot/main.py
```

---

## 📝 Примечания

- Бот будет автоматически перезапускаться при перезагрузке сервера
- Логи сохраняются в systemd journal
- База данных `users.db` создаётся автоматически в корне проекта
- Все временные файлы хранятся в папке `pers/users/`

---

## 🔐 Безопасность

1. **Не коммитьте `.env` файл в Git!** (он уже в `.gitignore`)
2. Регулярно обновляйте систему:
```bash
sudo apt update && sudo apt upgrade -y
```
3. Настройте firewall (если нужно):
```bash
sudo ufw allow 22/tcp
sudo ufw enable
```

